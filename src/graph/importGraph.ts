/**
 * Import graph builder stub.
 *
 * Sub-Task 3: returns an empty reverse import map.
 * Real implementation (madge + map inversion) added in Sub-Task 5.
 */

import type { ReverseImportMap } from '../types.js';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function buildImportGraph(_repo: string): Promise<ReverseImportMap> {
  return {};
}
